interface:
  display_name: "Modern Web Guidance"
  short_description: "Retrieve current web platform implementation guidance"
  default_prompt: "Use $modern-web-guidance to choose current browser APIs and compatible fallbacks for this web task."
policy:
  allow_implicit_invocation: true
